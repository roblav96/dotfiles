[filter "lfs"]
	clean = git-lfs clean -- %f
	process = git-lfs filter-process
	required = true
	smudge = git-lfs smudge -- %f
[user]
	email = roblav96@gmail.com
	name = Robert Laverty
[color]
	branch = always
	diff = always
	log = always
	status = always
	ui = always
[core]
	excludesfile = ~/.gitignore
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	tabs = 0
	line-numbers = true
	commit-decoration-style = 16 16 overline
	hunk-header-decoration-style = black box
	file-decoration-style = magenta box
	file-style = 231 bold
	minus-style = syntax "#3f0001" # 52
	minus-emph-style = syntax 88
	plus-style = syntax "#002800" # 22
	plus-emph-style = syntax 28
